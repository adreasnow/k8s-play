apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - tempo.yaml
  - gateway.yaml
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: tempo-values
    namespace: flux-system
    files:
      - values=values.yaml
