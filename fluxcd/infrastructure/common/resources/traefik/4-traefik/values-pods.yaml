deployment:
  replicas: 3 # match the number of AZs

# # One Traefik per AZ
# topologySpreadConstraints:
#   - maxSkew: 1
#     topologyKey: topology.kubernetes.io/zone
#     whenUnsatisfiable: DoNotSchedule
#     labelSelector:
#       matchLabels:
#         app.kubernetes.io/name: traefik

# Pod disruption budget
podDisruptionBudget:
  enabled: true
  minAvailable: 1
