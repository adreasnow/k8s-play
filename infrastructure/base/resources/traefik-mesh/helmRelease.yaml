apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik-mesh
  namespace: traefik-mesh
spec:
  interval: 10m
  chart:
    spec:
      chart: traefik-mesh
      version: 3.0.8
      sourceRef:
        kind: HelmRepository
        name: traefik
        namespace: flux-system
      interval: 1h
  install:
    createNamespace: true
  upgrade:
    cleanupOnFail: true
  values:
