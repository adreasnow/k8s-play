    apiVersion: kustomize.toolkit.fluxcd.io/v1
    kind: Kustomization
    metadata:
      name: traefik-core
      namespace: flux-system
    spec:
      interval: 10m
      timeout: 10m
      retryInterval: 1m
      path: ./fluxcd/infrastructure/common/resources/traefik/4-traefik
      prune: false
      force: false
      wait: true
      sourceRef:
        kind: GitRepository
        name: flux-system
      dependsOn:
        - name: traefik-tls-certificate
        - name: traefik-namespace
      postBuild:
        substituteFrom:
          - kind: ConfigMap
            name: cluster-vars
