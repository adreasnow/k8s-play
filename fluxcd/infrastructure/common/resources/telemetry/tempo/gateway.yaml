apiVersion: bellroy.com/v1alpha1
kind: MeshedService
metadata:
  name: tempo
  namespace: telemetry
spec:
  service: telemetry-tempo-gateway
  hostname: tempo.telemetry
  internal:
    http:
      enabled: true
      ports: [80]
  external:
    http:
      enabled: true
      ports: [80]
